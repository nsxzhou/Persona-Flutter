Run `specguard-finish`: confirm the spec check has run, decide whether the
completed work introduced durable knowledge, update `.specguard/spec/` when
the update is clear, and report verification plus spec-sync status.
